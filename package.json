{
  "name": "mezan",
  "private": true,
  "scripts": {
    "dev": "concurrently -n api,web -c blue,green \"npm run dev --prefix backend\" \"npm run dev --prefix web\"",
    "dev:backend": "npm run dev --prefix backend",
    "dev:web": "npm run dev --prefix web"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
